language: rust
rust:
  - nightly
matrix:
  allow_failures:
  - rust: nightly
  fast_finish: true
os:
- windows
- linux
- osx
deploy:
  provider: releases
  token: $GITHUB_TOKEN
  file: target/release/bibrust
  edge: true # opt in to dpl v2
  overwrite: true
  draft: true
  # on:
  #   tags: true